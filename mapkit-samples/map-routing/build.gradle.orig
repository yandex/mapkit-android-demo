plugins {
    id 'mapkit-samples.app'
}

android {
    namespace 'com.yandex.mapkitdemo.routing'
    buildFeatures {
        buildConfig true
    }
}

dependencies {
    // Add MapKit SDK full dependency
    implementation(name:'maps.mobile', ext:'aar')
}
