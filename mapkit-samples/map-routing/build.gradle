plugins {
    id 'mapkit-samples.app'
}

android {
    namespace 'com.yandex.mapkitdemo.routing'
}

dependencies {
    // Add MapKit SDK full dependency
    implementation "com.yandex.android:maps.mobile:${rootProject.ext.mapkitFullVersion}"
}
